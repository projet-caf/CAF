{% extends "::admin.html.twig" %}
{% block title "Menus" %}

{% block menu %}
	
{% endblock menu %}

{% block titlesection "Menus" %}

{% block body %}
{% if id != 0 %}
	<form action="{{ path('edit_menu_taxonomy', {'id' : id }) }}" method="post" {{ form_enctype(form) }}>
{% else %}
	<form action="{{ path('new_menu_taxonomy') }}" method="post" {{ form_enctype(form) }}>
{% endif %}		
    {{ form_widget(form) }}
    {{ form_rest(form) }}
    <a href="{{ path('menus_taxonomy') }}" class="btn">Fermer</a>
    <button type="submit" class="btn btn-primary">Enregistrer</button>
</form>
{% endblock body %}